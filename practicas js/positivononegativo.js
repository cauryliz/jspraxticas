let numero = prompt('Ingresa un numero');

if (numero >= 0) {
    alert(`El numero es ${numero == 0 ? 'El numero es cero' : 'El numero es positivo'}`);
} else {
    alert('El numero es negativo');
}; 